@inherits LayoutComponentBase

<div class="page">
    <main>
        @Body
    </main>
</div>

<style>
    /* Estilos mínimos para páginas públicas (Login/Register) */
    .page {
        min-height: 100vh;
        display: flex;
        flex-direction: column;
    }

        .page main {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100%;
        }
</style>

